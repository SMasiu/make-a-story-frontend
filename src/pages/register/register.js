import React from 'react';

import RegisterContent from '../../components/register-content/register-content';

const Register = ({history}) => {
    return (
    <section>
        <RegisterContent history={history}/>
    </section>)
}

export default Register;